# FINAL_PRODUCTION_AUDIT.md

> Execute-readiness audit performed at the end of the MASTER LOOP pass (2026-08-05) against `D:\c\coaching\second`. Each area is PASS (verified), CONDITIONAL (works but needs an env/task before shipping), or FAIL/ACTION REQUIRED.

## Verification summary

| Checks | Result |
|---|---|
| FE typecheck `npx tsc --noEmit -p tsconfig.json` | PASS |
| BE typecheck `npx tsc --noEmit -p apps/backend/tsconfig.json` | PASS |
| Production build `npm run build` (sitemap + vite + precache) | PASS |
| Dead-scaffolding removal (verified zero imports) | PASS |
| Backend test suite (vitest, 81 pass / 1 Db-gated skip) | unchanged this pass |

## Security
- **PASS** — Supabase JWT verification, server-side role resolution, object-level authorization (batch/ownership), fail-closed 503 in prod for offline mock.
- **PASS** — helmet (CSP off), CORS origin allowlist (production requires `Origin`), JSON 10 MB cap.
- **PASS** — rate limiting 500/15min API, 20/15min auth, 10/15min public enquiries.
- **PASS** — private uploads authorized per role; `/api/uploads` blocks `/private/`; attachment+nosniff.
- **PASS** — media deletion reference-checked (409 with usage summary); notices/events soft-delete (archive) preserves audits.
- **PASS** — audit logging on admin writes incl. CMS save/publish/restore; version journal is append-only.
- **CONDITIONAL** — no secrets in code; runtime creds sourced from `.env`/env vars. Confirm `SITE_URL` and deploy-time secrets are set in the host environment.
- **ACTION REQUIRED** — prune unused deps (MUI, jsonwebtoken, bcryptjs, ws, pg) in the same change that enables watches/CI (dry `pnpm install` first).

## SEO / Public site
- **PASS** — public pages indexable; `robots.txt` allows site, blocks portals/API; canonical + sitemap link in `index.html`; per-page title/description/OG via `useSeo`; CMS `seo` defaults applied.
- **CONDITIONAL** — sitemap is a placeholder until `SITE_URL`/`VITE_SITE_URL` is set. Set it at deploy-time; rebuild; confirm `http://<origin>/sitemap.xml` + robots Sitemap line.
- **PASS** — CMS draft→publish never leaks drafts to the public API; draft preview is admin-only.

## Operational
- **PASS** — `/api/admin/system` gives safe health + counts (no secrets).
- **PASS** — maintenance mode toggle in Settings; public banner + admin bypass.
- **PASS** — CMS version history lets operators roll back a bad publish.
- **PASS** — CSV export for Students/Teachers works with current filters.
- **PASS** — admin command palette + global search make large datasets navigable.

## Known intentional residual
- PWA precache manifest is regenerated by the build; ensure the host serves `dist/`.
- Vercel `vercel.json` and Render `render.yaml` exist; Render backend not deployed this session.
- DB migrations `0007` (CMS/media) and `0008` (cms_versions) must be applied to the live Supabase project before the new endpoints work (`supabase db push` / drizzle push). This is the **single required pre-deploy step**.

## Verdict
**Ready to ship to staging** after: (1) apply migration `0008` (and verify `0007` applied), (2) set `SITE_URL`, (3) run the three verification commands once more in a clean environment. Remaining items in FEATURE_GAP_REPORT (bundle splitting, reset-password, teacher/student audit, CI, Playwright) are backlog, not blockers.